# 个人主页 · 操作手册

> 给未来的你 / 跟你一起工作的 AI 看。出错了翻这里。

---

## 一分钟速查

| 想干啥 | 怎么干 |
|---|---|
| 改一句话 / 改 tagline / 改某个项目描述 | 开生产 `/admin` → 改 → 保存 → 刷新主页 ✓ |
| 加一篇新博客文章 | 改本地 `scripts/import-woshipm.ts` 加一条 → `git push` → 等 Railway 部署 → 开 `/admin` 点「灌种子数据」按钮 |
| 改样式 / 配色 / 加新页面 | 本地 `npm run dev` → 看效果 → `git push` → Railway 自动部署(1-3 分钟) |
| 部署后页面没变 | Railway → Deployments tab 看 build 进度,等绿(ACTIVE) |
| /admin 登不进去 | Railway → Variables 看 `ADMIN_PASSWORD` 是不是设了 |
| 生产 URL 显示「数据库还没灌数据」 | 开 `/admin` 登录 → 右上角点「灌种子数据」按钮 |

---

## 关键 URL / 路径

| 是啥 | 在哪 |
|---|---|
| 本地代码目录 | `/Users/sakura/sandbox/profile` |
| GitHub repo(私有)| https://github.com/irismayrain/profile |
| Railway 项目 | https://railway.app(用 GitHub 登录) |
| 生产网址 | https://irismayrain.com |
| 生产后台 | https://irismayrain.com/admin |
| 本地 dev | http://localhost:3002 (因为 3001 被 Axin 占了) |
| 本地后台 | http://localhost:3002/admin |
| 本地 admin 密码 | 看本地 `.env.local` 的 `ADMIN_PASSWORD` |
| 生产 admin 密码 | Railway → Variables tab 里的 `ADMIN_PASSWORD` |

---

## 项目地图

```
profile/
├── app/                          # Next.js 14 App Router
│   ├── page.tsx                  # 主页(Hero / Now / Connect)
│   ├── about/page.tsx            # 自述长信
│   ├── experience/page.tsx       # 个人优势 + 工作经历 + 教育
│   ├── projects/page.tsx         # 7 个项目 + 技能
│   ├── blog/                     # 文章列表 + 详情
│   ├── contact/page.tsx          # 联系
│   ├── admin/
│   │   ├── page.tsx              # 后台入口(server, 拉数据)
│   │   ├── AdminClient.tsx       # 后台 UI(client, 编辑器)
│   │   └── login/page.tsx        # 登录页
│   ├── api/admin/                # 后台 API(profile/exp/projects/articles/seed-all)
│   ├── globals.css               # 全站样式 + 字体 + lav 配色
│   └── layout.tsx                # 全局 head + title
│
├── components/
│   ├── SiteNav.tsx               # 顶部导航(自述/经历/项目/文章/联系)
│   └── Footer.tsx
│
├── lib/
│   ├── db.ts                     # SQLite 连接 + migrate + 类型 + 查询函数
│   └── auth.ts                   # admin 密码登录 + cookie
│
├── scripts/
│   ├── seed-iris.ts              # 灌入 profile / 经历 / 项目 / strengths / skills(UPSERT)
│   ├── import-woshipm.ts         # 灌入 5 篇博客文章(UPSERT by slug)
│   └── seed.ts                   # 老的 林夕 模板(留着没用)
│
├── data/                         # SQLite 数据库文件(本地存这里,生产挂卷)
│   └── site.db                   # 实际数据库,.gitignore 排除
│
├── public/
│   ├── vibecoding.html           # 你的 Vibe Coding 演讲稿
│   └── vibecoding.pdf            # PDF 版
│
├── .env.local                    # 本地环境变量(密码等,.gitignore 排除)
├── .gitignore                    # 已配好(env / db 文件 / node_modules 都排除)
└── package.json                  # tsx 在 dependencies 里(部署用)
```

---

## 日常工作流

### A. 改一段文字 / 数字 / 标签(最高频)

**直接在生产 /admin 改**,不用碰代码,改完即时同步:

1. 开 https://irismayrain.com/admin
2. 用 Railway → Variables 里的 `ADMIN_PASSWORD` 登录
3. 顶部切 tab:**个人信息 / 工作经历 / 项目 / 文章**
4. 找到要改的 → 编辑 → 保存
5. 开主页刷新,内容立刻变

**支持编辑的字段**:
- 个人信息 tab:name / tagline / bio / location / email / 头像 emoji / 自我介绍 / Manifesto / Now / 社交 / 教育 / **个人优势(STR 卡)** / **技能**
- 工作经历 tab:每段的公司 / 职位 / 时间 / 工作内容 + **Highlights 分项要点**(含可选 link 角标)
- 项目 tab:title / subtitle / 描述 / tags / 链接 + **Highlights**(含 link)
- 文章 tab:slug / 标题 / 摘要 / Markdown 正文 / 发布日期 / 是否发布

### B. 加一篇新博客文章

文章数据存在 `scripts/import-woshipm.ts` 数组里(代码层管理,不在 /admin 里加)。

1. 本地打开 `/Users/sakura/sandbox/profile/scripts/import-woshipm.ts`
2. 在 `articles` 数组里加一条:
   ```ts
   {
     slug: "your-english-slug",
     title: "你的文章标题",
     excerpt: "一句钩子摘要。",
     published_at: "2026-XX-XX",
     content: `Markdown 正文 ...
   
   ## 二级标题
   
   正文段落。`,
   },
   ```
3. 提交 + 推送:
   ```bash
   git add scripts/import-woshipm.ts
   git commit -m "add: 文章 your-english-slug"
   git push
   ```
4. **Railway 自动 build**(看 Deployments tab,1-3 分钟变绿)
5. 等 ACTIVE 后,开 `/admin` 点右上角「**灌种子数据**」按钮
6. 等 5-15 秒,看到 JSON 日志 `"ok": true` → 关掉
7. 开 `/blog`,新文章出来了

> 注意:`/admin` 的"灌种子数据"按钮会把 `scripts/seed-iris.ts` 也跑一遍。这两个脚本都是 UPSERT,**不会重复 insert**,也不会破坏你在 /admin 里手动改的内容(seed-iris 的字段会被代码版本覆盖,这是预期行为)。

### C. 改代码 / 样式 / 加新页面

1. **本地起 dev**:
   ```bash
   cd /Users/sakura/sandbox/profile
   npm run dev
   ```
   开 http://localhost:3002 看(不是 3001 —— 3001 被 Axin 占了。如果你停了 Axin,可以改回 3001)。
2. 改代码,浏览器自动热重载,实时看效果
3. 满意了:
   ```bash
   git add .
   git commit -m "feat: 描述你做了啥"
   git push
   ```
4. **Railway 自动检测到 push,开始 build**
5. 1-3 分钟后 ACTIVE,生产网站更新

### D. 改 schema / 数据库结构(进阶)

改 `lib/db.ts` 里的 `migrate()` 函数,加 `ALTER TABLE` 语句。

- 加新列:用 `ALTER TABLE ... ADD COLUMN ...`(在条件块里 `if (!cols.has("xxx"))`)
- 永远不要 `DROP TABLE` 或不带条件的 ALTER —— 会丢生产数据

push 完 Railway redeploy,新代码跑 `migrate()` 时会自动升级 schema(向后兼容)。

---

## 出错对照表

### 部署 / Railway 类

| 症状 | 原因 + 处理 |
|---|---|
| 主页显示「Application error: a client-side exception」 | 多半是某个组件拿到 null / undefined 字段。开浏览器 F12 → Console 看红字。 |
| 主页显示「数据库还没灌数据」 | 进 /admin 点「灌种子数据」按钮 |
| /admin 显示登录页但密码不对 | Railway → Variables 重置 `ADMIN_PASSWORD`,等 redeploy |
| /admin 改完保存,刷新主页没变 | 主页是 `force-dynamic` 应该即时更新。强刷新(Cmd+Shift+R)。如果还是没变 → Railway 重启 service |
| Railway build 红色失败 | Deployments → 点 build → View logs。最常见:依赖版本冲突,或 better-sqlite3 native 编译失败 |
| Railway 部署成功但页面 502/503 | 应用还在启动,等 30 秒-1 分钟 |
| 改了 /admin 内容,Railway redeploy 后丢失了 | 没挂卷!Railway → service → Settings → Volumes,确认有 `profile-volume` mount 在 `/app/data` |

### 本地开发类

| 症状 | 原因 + 处理 |
|---|---|
| `EADDRINUSE :3001` | 3001 被别的进程占了(常是 Axin 项目)。改 `package.json` 的 `"dev"` 端口,或 `lsof -i :3001` 找出 PID 用 `kill` 干掉 |
| TypeScript 报错(红波浪线) | `npx tsc --noEmit` 跑一遍看具体哪行。Iris 这个项目 schema 改了的话,看 `lib/db.ts` 类型是不是同步 |
| `npm run seed:iris` 把文章清了 | **不应该再发生了**(已修复)。如果发生,重跑 `npm run import:articles` 找回 |
| /admin 不能保存 | 看浏览器 Network tab,是不是 401(密码失效,重新登)或者 500(API 报错) |

### Git / GitHub 类

| 症状 | 原因 + 处理 |
|---|---|
| `git push` 被 reject(`fetch first`) | 远程有你没拉的 commit。`git pull --rebase` 然后 `git push` |
| `git push` 要密码,输了正确密码还不行 | GitHub 不收登录密码,要用 **Personal Access Token**:https://github.com/settings/tokens → 生成新 token,scope 选 `repo`。Username 用 `IrisMayrain`,Password 粘 token |
| 凭据错了,Mac 没再问 | Keychain 缓存了错凭据。清掉:`printf "protocol=https\nhost=github.com\n\n" \| git credential-osxkeychain erase` |

### Railway CLI 类

| 症状 | 原因 + 处理 |
|---|---|
| `railway run` 报 `tls handshake eof` | 国内网络拽 Cloudflare,装 VPN 或者**直接用 /admin 的「灌种子数据」按钮**(不依赖 CLI) |
| `railway run npm run X` 看似运行但写到本地 DB | 这是 `railway run` 的工作原理——**在本机跑命令,只是注入 Railway env vars**。**不会**在生产服务器跑。要在生产跑命令只能用 /admin 的「灌种子数据」按钮 或 Railway 网页 Shell |

---

## 数据库 schema 速记

> 改前先看,别瞎 ALTER。

### profile(单行,id=1)
- `name` text — 名字
- `tagline` text — 一句话定位
- `bio` text — 自我介绍(多行)
- `location` text — 所在地
- `avatar_emoji` text — 头像 emoji
- `email` text
- `socials` JSON `[{label, url}]`
- `manifesto` text — 信念金句
- `now_items` JSON `[{label, body}]` — 主页 NOW 卡(BUILDING / WRITING / OPEN TO)
- `strengths` JSON `[{kicker, title, tagline, body}]` — /experience 顶部 STR 卡
- `skills` JSON `[{label, items}]` — /projects 底部技能表
- `education` text — /experience 底部教育行

### experience(多行)
- `company` / `role` / `period` / `summary` text
- `highlights` JSON `[{tag, body, link?: {url, label}}]` — 分项要点
- `sort_order` int — 数字小的在前

### projects(多行)
- `title` / `subtitle` / `description` text
- `highlights` JSON 同上
- `tags` JSON `string[]`
- `link` text — 项目链接
- `sort_order` int

### articles(多行)
- `slug` text UNIQUE — URL 友好的英文 slug
- `title` / `excerpt` / `content` text
- `published_at` text — `YYYY-MM-DD`
- `is_published` int(0/1)

---

## 命令速查

```bash
# 本地开发
npm run dev                       # 起 dev server(localhost:3002)
npm run build                     # 测一下 production build 能不能过
npm run seed:iris                 # 本地 DB 灌种子数据
npm run import:articles           # 本地 DB 灌 5 篇文章
npx tsc --noEmit                  # 类型检查

# Git
git status                        # 看改了啥
git add . && git commit -m "..."  # 提交
git push                          # 推送(触发 Railway 部署)
git pull --rebase                 # 远程有更新时先 rebase 拉下来
git log --oneline -5              # 看最近 5 个 commit

# Railway CLI(可选,容易拽)
railway login                     # 登录
railway link                      # 关联到项目
railway logs                      # 看生产日志(可能拽)

# 一些 debug 命令
lsof -i :3001                     # 谁占了 3001 端口
lsof -i :3002                     # 谁占了 3002
npx kill-port 3001                # 强杀占 3001 的进程
```

---

## 偶尔会用到

### 本地 DB 完全重置

```bash
rm data/site.db data/site.db-wal data/site.db-shm
npm run dev      # 起一次,会自动 migrate 出空 DB
npm run seed:iris && npm run import:articles
```

### 看生产 DB 内容(进 Railway 容器看)

Railway → service → Deployments → 最新一条 → `⋮` 菜单(如果有的话)→ Open Shell → 在容器里:

```bash
sqlite3 /app/data/site.db
.tables                            # 列所有表
SELECT * FROM profile;             # 查看 profile
SELECT slug, title FROM articles;  # 查看文章列表
.quit
```

### 备份生产 DB

```bash
# 用 Railway CLI 把生产 DB 拉到本地(如果 CLI 能通的话)
railway shell    # 进容器
# 在容器里:cat /app/data/site.db | base64 > /tmp/db.b64
# 然后 download

# 或者最简单:Railway 给 Volume 提供自动 Backups(看 service 顶部 Backups tab)
```

---

## 这套架构的几个核心决定(给未来的你)

1. **SQLite + 单文件 DB** —— 不要切到 Postgres 除非用户量到 1000+。SQLite 跟 Next.js 同进程跑,无网络延迟,简单到爆
2. **Railway + 持久卷** —— 不要部署到 Vercel,Vercel 文件系统不持久,SQLite 会被擦掉
3. **内容真相 = 代码 + /admin 混合** ——
   - profile / experience / projects 字段:**/admin 是真相**(可随时改,即时同步)
   - articles:**`scripts/import-woshipm.ts` 是真相**(代码层,改完 push + 点「灌种子数据」)
   - 不要在 /admin 直接改文章正文(可以但不推),除非临时改个错字
4. **`scripts/seed-iris.ts` 是首次部署 + 修复内容用的** —— 平时用 /admin 改单字段;只有需要重置 / 批量更新 strengths 这类结构化数据时,才改 seed 然后点「灌种子数据」按钮

---

## 找谁

- 卡住 → 回来翻这个文件
- 这个文件没写 → 跟 Claude / 其他 AI 助手描述具体症状 + 把"出错对照表"贴给它

写于 2026-05-24。
